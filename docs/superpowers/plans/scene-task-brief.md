# Scene task requirements

Repo C:/Users/admin/Documents/graduation project/GRAD-project, feature branch JMF-2340550-sep17. User approved implementation of an Arabic engineering replay lab with Three.js low-poly isometric Toyota GR Supra, inspired by AI Town (pastel sage miniature terrain, stylized trees/buildings, warm white UI and soft shadows). You own ONLY app/static/sim/scene.mjs and optional geometry.mjs / scene.test.mjs. Root writes all HTML/CSS/playback/Python/package files. Do not spawn other agents. No commits, no network installs; dependency will be available at app/node_modules/three version 0.180.0 shortly.

Exports:
```js
export function createScene(host) // returns update(frame,presentation), setFollow(boolean), resetCamera(), zoom(factor), dispose()
export function createEngineScene(host) // returns update(frame), dispose()
```
Use imports `three` and `three/addons/controls/OrbitControls.js`; import map provided by root. Main host ~850x520 desktop; engine host ~300x200. Root calls update every rAF; module must render on update and resize (no duplicate internal animation loop). frame is null before data ready, otherwise {t,s_m,speed_kmh,rpm,gear,gear_source,grade_pct,coolant_c,oil_c,turbine_c,turbine_lo_c,turbine_hi_c,block_c,fuel_gps,torque_nm,map_kpa,ok,gap}. Numeric channels can be null. presentation = {distance_m,preview_m,playing}. Use ONLY presentation distance for driving motion, no clock-generated movement. Before data, park the car visibly at start. Illustrative road loop with flat, ascent, descent, never feed elevation into physics. Distance->arc position must be continuous, scaled meaning explicitly exported or in comment for UI documentation. Missing distance freezes car; preview null hides highlight. Road preview highlight should visibly follow car along road, use real replay preview_m scaled consistently with distance and clipped at loop length. Avoid entire loop uniformly lit for normal ~1km preview; choose reasonable world units / distance scale. Supra should clearly read as sports coupe (long sculpted hood, low roof, wide rear, four wheels, lamps, rear spoiler). Car noticeable in overview: hero foreground composition, 10-18% scene width is fine stylization. Cream/white body, dark windows, muted accent. Roads charcoal with warm ivory edge markings. Terrain island plus sparse designed roadside landscape, no text requiring texture downloads. Vehicle pitched with terrain travel. Render soft shadows, ambient illumination. Low-poly materials, no external assets, no giant blank scene.

Engine inset is a schematic inline-six block + cylinder head, exhaust manifold, single turbo turbine/compressor linked by shaft, pipes and radiator cooling loop. No invented readings: color block with frame.block_c, turbine with frame.turbine_c, coolant loop via frame.coolant_c; gray for unavailable. Separate oil small component via frame.oil_c. Radiator is a proxy colored by coolant, not separately modeled metal. Internal rotation optional only if based on replay t/rpm; don't imply exact crank phase.

Use orthographic camera, OrbitControls for drag rotation/wheel zoom, explicit setFollow and resetCamera. Camera follow shouldn't fight user orbit or jump abruptly at loop boundary. Cap pixel ratio <=2, shadows <=2048; resize observers, dispose all geometries/materials/renderers/listeners. Honor reduced-motion by avoiding optional ambient motion. Null-safe defaults, WebGL errors propagate to root for UI handling. Useful pure geometry tests with Node test runner for arc continuity are welcome; no tautological tests for colors/layout. Root will browser-check.

Write report to docs/superpowers/plans/scene-task-report.md: owned files, interface details, tests, distance scaling, remaining limitations. Return only status + concise summary. The user already approved the visual design; do not ask them to approve again.
